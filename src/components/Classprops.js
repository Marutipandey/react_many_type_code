import React, { Component } from 'react'

export default class Classprops extends Component {
  render(props) {
    return (
      <div>
        <p>class props {this.props.name}</p>
      </div>
    )
  }
}
