function Functionprops(props){
    return(
        <div>
        <h1>{props.name} {props.email} navbar page</h1>
        <h2>{props.other.roll}</h2>
        </div>
       

    )
} 
export default Functionprops;